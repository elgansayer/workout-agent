name: Retired GitHub-Hosted Auto Dispatcher

# Issue selection and dispatch belong to the OpenHands control plane on the
# VPS. This GitHub-hosted autonomous dispatcher is retained only as an explicit
# retirement record.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The GitHub-hosted auto-dispatcher is retired; no work was dispatched."
