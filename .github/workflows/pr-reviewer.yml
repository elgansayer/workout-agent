name: Retired PR Reviewer Agent

# GitHub-hosted autonomous review is retired. Review and repair remain on the
# canonical issue branch under the Repo Factory/OpenHands VPS control plane.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The GitHub-hosted PR reviewer is retired; no review was authored."
