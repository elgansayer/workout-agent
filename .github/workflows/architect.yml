name: Retired GitHub-Hosted Architect

# Architecture and gap-analysis decisions belong to the OpenHands control plane
# on the VPS. This GitHub-hosted autonomous author is retained only as an
# explicit retirement record.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The GitHub-hosted architect is retired; no repository changes were made."
