name: Retired GitHub-Hosted AI Architect

# Product planning and backlog expansion are owned by the OpenHands Agent
# Canvas daemon on the VPS. This workflow must not create tickets or consume
# Claude/API credentials from GitHub Actions.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The GitHub-hosted AI architect is retired."
          echo "No repository files or GitHub issues were changed."
