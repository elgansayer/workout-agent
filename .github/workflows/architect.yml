name: Retired Architect Agent

# GitHub-hosted architecture work is retired. Repo Factory and OpenHands Agent
# Canvas on the VPS own issue-scoped implementation and product decisions.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The GitHub-hosted architect agent is retired; no changes were made."
