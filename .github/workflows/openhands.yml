name: Retired GitHub-Hosted OpenHands Agent

# OpenHands runs through the single control plane on the VPS. This former
# GitHub-hosted autonomous coding workflow is retained only as an explicit
# retirement record.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The GitHub-hosted OpenHands agent is retired; no code was authored."
