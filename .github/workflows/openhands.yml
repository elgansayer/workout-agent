name: Retired GitHub OpenHands Agent

# GitHub-hosted autonomous implementation is retired. OpenHands Agent Canvas
# runs only through the repository's single control plane on the VPS.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The GitHub-hosted OpenHands agent is retired; no code was changed."
