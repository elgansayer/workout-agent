name: Retired Agent Weekly Tasks

# Recurring issue generation is retired. The OpenHands daemon on the VPS
# owns scheduling and should work from the curated product backlog instead.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Weekly GitHub issue generation is retired; no issues were created."
