Add ENCRYPTION_KEY, WEB_AUTH_SECRET, ALLOWED_EMAILS, WEB_GOOGLE_CLIENT_ID, and WEB_GOOGLE_CLIENT_SECRET to config.py's Config dataclass so web-auth/encryption configuration has one source of truth instead of being split between config.py and ad hoc os.environ reads in webapp/app.py and encryption.py.
