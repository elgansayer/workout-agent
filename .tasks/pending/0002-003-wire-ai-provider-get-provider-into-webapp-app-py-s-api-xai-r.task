Wire ai_provider.get_provider() into webapp/app.py's /api/xai_reasoning, /api/project_peak, and /api/rag_search endpoints so they resolve the logged-in user's preferred AI provider/key from Settings instead of hardcoding Gemini, per the ai-provider-wiring skill.
